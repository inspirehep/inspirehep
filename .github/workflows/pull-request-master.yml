name: Pull request master

on:
  pull_request:
    branches: [master]


jobs:
  test:
    uses: ./.github/workflows/integration-tests.yml
    with:
      ref: ${{ github.head_ref }}
    secrets: inherit
