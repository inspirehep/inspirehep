import React from 'react';
import { connect } from 'react-redux';

function SearchPage() {
  return <div>Search</div>;
}

const mapStateToProps = () => ({});
const dispatchToProps = () => ({});

export default connect(mapStateToProps, dispatchToProps)(SearchPage);
